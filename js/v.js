var app;

function loadVue() {
	app = new Vue({
	    el: "#app",
	    data: {
            player,
            notate,
            changeTab,
            buyUPG,
            FORMULA,
            UPGRADE,
            TABS,
            ACHIEVEMENTS,
        }
	})
}